# Neon pooled connection (used by the Next.js app)
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"

# Neon direct connection (used by Prisma migrations)
DIRECT_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"

# Vercel Blob token for wish photos
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Session JWT (16+ characters)
AUTH_SECRET="replace-with-a-long-random-secret"

# Bootstrap admin account (instrumentation au boot, `npm run bootstrap:admin`,
# ou login du compte admin — idempotent, applique la rotation du mot de passe)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-me-please"

# Optionnel : origine publique utilisée pour construire les liens d'invitation
# (sinon, l'en-tête Host de la requête est utilisé)
APP_ORIGIN="https://veloura.example.com"
